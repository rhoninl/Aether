behavior BossPhase {
    @caps(Combat, Movement)
    version 1

    selector {
        damage(nearest_player, 30);
        damage(nearest_player, 20);
        move(self, vec3(0.0, 0.0, 0.0), 4.0);
    }
}
