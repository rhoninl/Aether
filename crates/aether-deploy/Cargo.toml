[package]
name = "aether-deploy"
version = "0.1.0"
edition = "2021"
description = "Deployment topology and infra configuration primitives"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"

