[package]
name = "aether-lua"
version = "0.1.0"
edition = "2021"
description = "Lua scripting runtime for the Aether VR engine"

[dependencies]
aether-scripting = { path = "../aether-scripting" }
mlua = { version = "0.10", features = ["lua54", "vendored", "send"] }

[dev-dependencies]
