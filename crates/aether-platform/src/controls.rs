#[derive(Debug)]
pub enum FidelityMode {
    Full,
    Balanced,
    Low,
    Accessibility,
}

#[derive(Debug)]
pub enum SceneScaleMode {
    Metres,
    Scaled,
}

#[derive(Debug)]
pub enum VisualMode {
    Standard,
    Spectator,
}

