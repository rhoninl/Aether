[package]
name = "aether-renderer"
version = "0.1.0"
edition = "2021"
description = "Rendering policy primitives for the Aether VR engine"

[features]
default = []
gpu-tests = []

[dependencies]
wgpu = "24"
bytemuck = { version = "1", features = ["derive"] }
log = "0.4"
