[package]
name = "aether-world-runtime"
version = "0.1.0"
edition = "2021"
description = "World runtime loading, streaming, and lifecycle primitives"

[dependencies]
aether-canonical-shim = { path = "../aether-canonical-shim" }
thiserror = "1"
tracing = "0.1"
